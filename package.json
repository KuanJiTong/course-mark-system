{
  "scripts": {
    "frontend": "cd frontend && npm run serve",
    "backend": "php -S localhost:3000 -t backend/public",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1"
  }
}
